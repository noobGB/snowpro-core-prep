# Docs Map — what documents which part of the system

This repo has four doc files (`README.md`, `TECH_STACK.md`, `CLAUDE.md`, `mcp-server/README.md`)
plus `.github/screenshot-*.png`, and each one describes several unrelated parts of the system. That
made it easy for a real feature (LAN multi-user login, #37/#41) to ship correctly while three of
those docs quietly kept describing the *previous* architecture — not missing new content, but
**actively wrong** in places (README claimed "no account," TECH_STACK.md claimed "no database, no
auth system"). Caught and fixed once (#44); this file exists so the next change finds its doc debt
before merge, not after a "haven't we forgotten something" prompt.

**How to use this**: before merging any change, find the row(s) below matching what you touched,
and open every doc cell listed — confirm it's still accurate, don't assume "I didn't touch the docs
so they're fine." A row with no cell changed needing an edit is a fine outcome; a row never checked
is the actual failure mode this file exists to prevent. When you add a genuinely new subsystem/topic
that doesn't fit an existing row, add a new row here in the same PR — this table is itself subject
to the rule it describes.

| Topic | Source of truth (code) | Docs to check |
|---|---|---|
| **Identity / login / password / Google OAuth / multi-user (LAN) / forgot-password (email) / admin roles** | `pipeline/src/db.ts`, `pipeline/src/passwords.ts`, `pipeline/src/oauth.ts` (issue #113 — Google OAuth, `SNOWPRO_GOOGLE_CLIENT_ID`/`SECRET`), `pipeline/src/mailer.ts` (issue #59 — email via Brevo's HTTP API using `SNOWPRO_EMAIL_*`, switched from SMTP by issue #95 since Railway blocks outbound SMTP ports below its Pro plan; issue #62 adds `sendAdminCreatedAccountEmail`; issue #93 adds `sendWelcomeEmail`), `pipeline/src/server.ts` (`/api/session`, `/api/me`, `/api/logout`, `/api/account/password[-setup]`, `/api/password-reset/[request\|confirm]`, `/api/oauth/google/[start\|callback\|available]` (issue #113), `/api/admin/*` routes, `requireAdmin`), `app/src/lib/session.ts`, `app/src/lib/admin.ts` (issue #62), `app/src/components/LoginGate.tsx` (issue #113 adds the "Continue with Google" button/availability check), `app/src/components/ResetPasswordPage.tsx`, `app/src/components/PasswordInput.tsx`, `app/src/components/SettingsPanel.tsx` (Profile section), `app/src/pages/Admin.tsx` (issue #62), `app/src/components/Sidebar.tsx` (Admin nav link, desktop), `app/src/components/MobileMoreSheet.tsx` + `app/src/components/MobileBottomNav.tsx` (Admin nav link, mobile — separate component tree from Sidebar.tsx, doesn't inherit its changes for free), `pipeline/scripts/admin-users.mjs` (operator CLI — list/remove/promote/demote/reset-all against the same `users`/`sessions`/`progress`/`password_resets` tables; every child table must be cleared on delete, see issue #175), `.env.example` | README.md's `## Multi-user (LAN)` (including its admin-CLI, forgot-password, Google sign-in, and admin bullets), `## Features` (Profile/Settings bullets), intro paragraph, `## Architecture`; TECH_STACK.md `## 5. Backend`; CLAUDE.md's Identity + Password login + Google OAuth login (issue #113) + Self-service password reset + Admin roles/page + Adding a user as admin + Admin CLI paragraphs under `## Frontend app (app/)`, and its `## Running it` `.env.example` + `SNOWPRO_GOOGLE_*` + `SNOWPRO_HOST_NAME`/`publicOrigin()` mentions |
| **Progress persistence / storage backend** (the `ProgressState` blob, HTTP vs. `localStorage`, the ETag/If-Match conflict protocol) | `app/src/lib/progress.ts`, `pipeline/src/server.ts` (`/api/progress` routes), `pipeline/src/db.ts` (progress table) | CLAUDE.md's Progress/persistence paragraphs under `## Frontend app (app/)`; TECH_STACK.md `## 5. Backend`; README.md's Settings → Backup/Reset bullets |
| **Theming (Light/Dark)** | `app/src/lib/theme.ts`, `app/src/styles/tokens.css`, `app/src/components/SettingsPanel.tsx` (Appearance section) | README.md's Settings → Appearance bullet; CLAUDE.md's theme paragraph under `## Frontend app (app/)` |
| **Content pipeline** (markdown → `content.json`/notes JSON/search index) | `pipeline/src/index.ts`, `pipeline/src/parsers/*`, `pipeline/src/assemble/*`, `pipeline/src/write/*` | CLAUDE.md `## Content pipeline (pipeline/)`; TECH_STACK.md `## 2. The content pipeline`; README.md `## Adding or editing content` and its `### Domain notes`/`### Practice questions`/etc. subsections |
| **Quiz runner, scoring, readiness** | `app/src/pages/Runner.tsx`, `app/src/lib/scoring.ts`, `app/src/lib/readiness.ts` | CLAUDE.md's Readiness paragraphs under `## Frontend app (app/)`; README.md's `## Features` (Practice/Mock exams/Analytics bullets) |
| **Analytics — timing chart / per-question deep link** (issue #81: per-question `timeSec` capture, `slowestQuestions()`'s latest-per-question dedup across all attempts, the "Time per question" chart's absolute time scale + scrollable uncapped list, and its `#question-<id>` deep link into Results) | `app/src/pages/Runner.tsx` (`finalize()` — writes each question's own `answers[qid].timeSec`, not one shared elapsed-at-submit stamp), `app/src/lib/timing.ts` (`slowestQuestions()`), `app/src/pages/Analytics.tsx` (`TimingView`, `TIME_SCALE_MAX_SEC`), `app/src/pages/Results.tsx` (`ReviewCard`'s `id`/`highlighted`, the scroll/focus/auto-open effect) | CLAUDE.md's "Per-question timing and Analytics' 'Time per question' chart" paragraph under `## Frontend app (app/)`; `.github/screenshot-analytics.png` |
| **Study plan compression / crunch mode** (linear vs. crunch remap, per-task must/skippable tiers, the Mock 1 → gap → Mock 2 invariant, pinned exam registration — issue #76) | `app/src/lib/planDates.ts` (`buildPlan()`; `remapPlan()` is also used directly by `mcp-server/src/session.ts`'s `get_study_plan` — don't rename/change its signature without updating that call site too), `pipeline/src/parsers/studyPlan.ts` (`{skip-ok}`/`{pin-early}`/`{mock:1}`/`{mock:2}`/`{review}` tag parsing) | CLAUDE.md's `src/parsers/studyPlan.ts` bullet under `## Content pipeline`; `claude_plans/snowpro-crunch-mode-study-plan.md` (full bucketing table); `app/src/pages/StudyPlan.tsx` and `Dashboard.tsx`'s own rendering of `buildPlan()`'s output (PR2) |
| **MCP server** | `mcp-server/src/*` | `mcp-server/README.md` (all sections — it's the primary doc); CLAUDE.md `## MCP server (mcp-server/)`; TECH_STACK.md `## 6. The MCP server`; README.md `### Option C — MCP server` |
| **Docker / Compose / deployment shape** | `Dockerfile`, `docker-compose.yml` | CLAUDE.md `## Running it`; TECH_STACK.md `## 7. Containerization`; README.md's install instructions (`### Option A`, including its GHCR-pull callout — see the CI/CD row below for the workflow that publishes it) |
| **Windows one-click launcher** | `Launch-SnowPro.ps1` | README.md's "Windows one-click launcher" callout under `### Option A`; CLAUDE.md `## Running it` |
| **CI/CD** (`.github/workflows/*`, Dependabot) — includes `release.yml`'s GHCR publish (issue #74) | `.github/workflows/*.yml`, `.github/dependabot.yml` | CLAUDE.md `## CI/CD (.github/)`; TECH_STACK.md `## 9. CI/CD`; README.md's GHCR-pull callout under `### Option A` |
| **Development workflow itself** (issue-first, branch/PR/CI-gate/merge) | N/A — process, not code | CLAUDE.md `## Development workflow` (source of truth); `CONTRIBUTING.md` (the human-facing summary of the same workflow — keep it in sync, don't let it drift into its own, different process); `.github/pull_request_template.md` and `.github/ISSUE_TEMPLATE/*` (the same workflow again, as a checklist people actually see — a third copy, so it drifts the same way) |
| **Security policy / self-host hardening guidance** | `pipeline/src/guestMode.ts` (`SNOWPRO_ENABLE_GUEST` default-off), `pipeline/src/db.ts` (`ensureConfiguredAdmins`) | `SECURITY.md` (scope, and the two variables that matter on a public instance); `.env.example` |
| **Permanent home page for public-host visitors / Open Graph meta tags** (issue #123, supersedes #121's one-time dismissible landing page — `LoginGate.tsx`'s form logic extracted as `AuthForm`, `HomePage.tsx`'s split-screen layout, the `isPublicHost` gate reusing #119's `isPrivateNetworkHost()`, the `og:*`/`twitter:*`/description meta tags) | `app/src/App.tsx` (`AuthState`, `isPublicHost`), `app/src/components/LoginGate.tsx` (`AuthForm` export + the `LoginGate` wrapper), `app/src/components/HomePage.tsx`, `app/src/styles/tokens.css` (`.home-*` grid/animation rules, `.skip-link`), `app/src/lib/session.ts` (`fetchMe()`'s `isPublicHost`), `pipeline/src/server.ts` (`GET /api/me`'s 401 body), `app/index.html` (meta tags — and, since #154, a `<noscript>` block duplicating the kicker/h1/pitch/`FEATURES` copy **verbatim** for non-JS crawlers: editing that copy in `HomePage.tsx` without editing `index.html` silently desyncs what a crawler reads from what a visitor sees, and nothing enforces it but this row), `app/vite.config.ts` (`copyLandingScreenshot()` — the screenshot's fixed un-hashed `dist/` root path), `app/src/assets/landing-dashboard.png` | README.md's `## Features` (Home page bullet), `.github/screenshot-home.png`; CLAUDE.md's "Permanent home page for public-host visitors (issue #123, supersedes #121's first cut)" paragraph under `## Frontend app (app/)` |
| **Any visible UI change** | whatever page/component changed | README.md's screenshot block (`.github/screenshot-*.png` + the `![...]` line above each) — a screenshot showing stale UI is exactly the kind of doc debt this file exists to prevent, same as stale prose |
| **System architecture (topology, not one feature)** | this table's own rows, `Dockerfile`, `docker-compose.yml`, `pipeline/src/server.ts`, `mcp-server/src/index.ts` | `docs/architecture.drawio` (+ `.drawio.png` / `-flow.svg` / `-flow.gif`, regenerate via the `drawio-skill`'s `svgflow.py` + `buildup.py --gif`) and README.md's `## Architecture` section — a new process/container/data-store or a changed request path (not a route added to an existing box) means the diagram is stale |
| **Public-facing summary / SEO & AI-agent discoverability** | this table's own rows (it's a summary of the same subsystems); `app/public/robots.txt` + its `ROOT_ASSETS` entry in `app/vite.config.ts` (issue #153 — crawl directives, incl. the deliberate `Disallow` on the answer bank at `/content.json`, `/notes/`, `/search-index.json`); `app/index.html`'s inline `application/ld+json` graph (issue #155 — WebSite + SoftwareApplication + Person/`hasCredential`; its `featureList` duplicates `HomePage.tsx`'s `FEATURES` titles verbatim and nothing but this row enforces that, same trap as the `<noscript>` block) | `llms.txt` (repo root — the whole-project summary an AI agent/crawler reads first) and the GitHub repo's own topics/description (`gh repo edit`, not a file) — a new top-level subsystem (a new `*/` package, a materially new feature area) means both are stale, same trigger as the System architecture row above. A new public URL namespace (or a newly public/newly gated path) means `robots.txt` is stale too. `pipeline/src/write/staticPages.ts` (issue #171) generates `/guide/`, the per-domain pages, `sitemap.xml` and the site-facing `llms.txt` at boot from `content.json` — note `/guide/*` is now a RESERVED URL namespace: a future SPA route there would be silently shadowed by the static file, since the pipeline output dir is mounted ahead of the catch-all in `server.ts` |

## What's deliberately not covered here

`SnowPro_Notes_and_Questions/CONTENT_FRESHNESS.md` and the content-freshness check
(`pipeline/npm run check:freshness`) are their own, separate staleness concern — they're about the
*Snowflake exam content* going stale relative to Snowflake's own docs, not this repo's own docs
going stale relative to its own code. Different problem, already has its own tooling; not
duplicated here.
